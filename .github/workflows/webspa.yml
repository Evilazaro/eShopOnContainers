name: webspa CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/webspa/**' # Branches containing feature/ in the name
      - 'bugfix/webspa/**' # Branches containing bugfix/ in the name
      - 'hotfix/webspa/**' # Branches containing hotfix/ in the name
      - 'release/webspa/**' # Branches containing release/ in the name
      - 'refactor/webspa/**' # Branches containing refactor/ in the name
      - 'test/webspa/**' # Branches containing test/ in the name
      - 'techdebt/webspa/**' # Branches containing techdebt/ in the name
    paths:
      - src/BuildingBlocks/**
      - src/Web/WebSPA/**
      - .github/workflows/webspa.yml

  pull_request: # Trigger the workflow on pull requests
    branches:
      - main
    paths:
      - src/BuildingBlocks/**
      - src/Web/WebSPA/**
      - .github/workflows/webspa.yml

env:
  SERVICE: webspa
  IMAGE: webspa

jobs:
  BuildContainersForPR_Linux:
    runs-on: ubuntu-latest
    # if: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - uses: ./.github/workflows/composite/build
        with:
          service: ${{ env.SERVICE }}
          # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}

  # BuildLinux:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event_name != 'pull_request' }}
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     - uses: ./.github/workflows/composite/build-push
  #       with:
  #         service: ${{ env.SERVICE }}
  #         registry_host: ${{ secrets.REGISTRY_HOST }}
  #         # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
  #         image_name: ${{ env.IMAGE }}
  #         registry_username: ${{ secrets.USERNAME }}
  #         registry_password: ${{ secrets.PASSWORD }}
