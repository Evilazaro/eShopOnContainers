name: mobileshoppingagg CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/mobileshoppingagg/**' # Branches containing feature/ in the name
      - 'bugfix/mobileshoppingagg/**' # Branches containing bugfix/ in the name
      - 'hotfix/mobileshoppingagg/**' # Branches containing hotfix/ in the name
      - 'release/mobileshoppingagg/**' # Branches containing release/ in the name
      - 'refactor/mobileshoppingagg/**' # Branches containing refactor/ in the name
      - 'test/mobileshoppingagg/**' # Branches containing test/ in the name
      - 'techdebt/mobileshoppingagg/**' # Branches containing techdebt/ in the name
    paths:
      - src/ApiGateways/Mobile.Bff.Shopping/aggregator/**
      - .github/workflows/mobileshoppingagg.yml

  pull_request: # Trigger the workflow on pull requests
    branches:
      - main
    paths:
      - src/ApiGateways/Mobile.Bff.Shopping/aggregator/**
      - .github/workflows/mobileshoppingagg.yml

env:
  SERVICE: mobileshoppingagg
  IMAGE: mobileshoppingagg

jobs:
  mobileshoppingagg-ci:
    runs-on: ubuntu-latest
    # if: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - uses: ./.github/workflows/composite/build
        with:
          service: ${{ env.SERVICE }}
          # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}

  # BuildLinux:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event_name != 'pull_request' }}
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - uses: ./.github/workflows/composite/build-push
  #     with:
  #       service: ${{ env.SERVICE }}
  #       registry_host: ${{ secrets.REGISTRY_HOST }}
  #       # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
  #       image_name: ${{ env.IMAGE }}
  #       registry_username: ${{ secrets.USERNAME }}
  #       registry_password: ${{ secrets.PASSWORD }}
