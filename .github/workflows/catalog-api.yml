name: catalog-api CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/catalog-api/**' # Branches containing feature/ in the name
      - 'bugfix/catalog-api/**' # Branches containing bugfix/ in the name
      - 'hotfix/catalog-api/**' # Branches containing hotfix/ in the name
      - 'release/catalog-api/**' # Branches containing release/ in the name
      - 'refactor/catalog-api/**' # Branches containing refactor/ in the name
      - 'test/catalog-api/**' # Branches containing test/ in the name
      - 'techdebt/catalog-api/**' # Branches containing techdebt/ in the name

    paths:
      - src/BuildingBlocks/**
      - src/Services/Catalog/**
      - .github/workflows/catalog-api.yml
  
  pull_request: # Trigger the workflow on pull requests
    branches:
      - main
    paths:
      - src/BuildingBlocks/**
      - src/Services/Catalog/**
      - .github/workflows/catalog-api.yml

env:
  SERVICE: catalog-api
  IMAGE: catalog.api
  DOTNET_VERSION: 7.0.x
  PROJECT_PATH: Services/Catalog/Catalog.API
  TESTS_PATH: Services/Catalog/Catalog.UnitTests

jobs:

  catalog-api-ci:
    runs-on: ubuntu-latest
    # if: ${{ github.event_name == 'pull_request' }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - uses: ./.github/workflows/composite/build-test
      with:
        service: ${{ env.SERVICE }}
        # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
        dotnet_version: ${{ env.DOTNET_VERSION }}
        project_path: ${{ env.PROJECT_PATH }}
        tests_path: ${{ env.TESTS_PATH }}

  # BuildLinux:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event_name != 'pull_request' }}
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - uses: ./.github/workflows/composite/build-push
  #     with:
  #       service: ${{ env.SERVICE }}
  #       registry_host: ${{ secrets.REGISTRY_HOST }}
  #       # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
  #       image_name: ${{ env.IMAGE }}
  #       registry_username: ${{ secrets.USERNAME }}
  #       registry_password: ${{ secrets.PASSWORD }}