name: ordering-backgroundtasks CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'feature/ordering-backgroundtasks/**' # Branches containing feature/ in the name
      - 'bugfix/ordering-backgroundtasks/**' # Branches containing bugfix/ in the name
      - 'hotfix/ordering-backgroundtasks/**' # Branches containing hotfix/ in the name
      - 'release/ordering-backgroundtasks/**' # Branches containing release/ in the name
      - 'refactor/ordering-backgroundtasks/**' # Branches containing refactor/ in the name
      - 'test/ordering-backgroundtasks/**' # Branches containing test/ in the name
      - 'techdebt/ordering-backgroundtasks/**' # Branches containing techdebt/ in the name
    paths:
      - src/BuildingBlocks/**
      - src/Services/Ordering/**
      - .github/workflows/ordering-backgroundtasks.yml

  pull_request: # Trigger the workflow on pull requests
    branches:
      - main
    paths:
      - src/BuildingBlocks/**
      - src/Services/Ordering/**
      - .github/workflows/ordering-backgroundtasks.yml

env:
  SERVICE: ordering-backgroundtasks
  IMAGE: ordering.backgroundtasks

jobs:
  BuildContainersForPR_Linux:
    runs-on: ubuntu-latest
    # if: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - uses: ./.github/workflows/composite/build
        with:
          service: ${{ env.SERVICE }}
          # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}

  # BuildLinux:
  #   runs-on: ubuntu-latest
  #   if: ${{ github.event_name != 'pull_request' }}
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - uses: ./.github/workflows/composite/build-push
  #     with:
  #       service: ${{ env.SERVICE }}
  #       registry_host: ${{ secrets.REGISTRY_HOST }}
  #       # registry_endpoint: ${{ secrets.REGISTRY_ENDPOINT }}
  #       image_name: ${{ env.IMAGE }}
  #       registry_username: ${{ secrets.USERNAME }}
  #       registry_password: ${{ secrets.PASSWORD }}
